version: '3.8'

services:
  # PHP 5.6 Application Service
  php-app:
    build:
      context: ./php-app
      dockerfile: Dockerfile
    container_name: php-app
    ports:
      - "8080:80"
    restart: unless-stopped